# 2026.06.11

## 1. 增强安装流程健壮性与错误处理

- `install.sh`: 完整 TLS 方案集成（Nginx/Caddy 安装与 systemd 服务注册）；下载失败提前退出并清理；_wget 代理导出完整环境变量；依赖包安装等待机制；jq 安装路径动态追踪
- `src/download.sh`: Caddy 校验和改用 GitHub Release API digest 字段为主，checksums.txt 解析为兜底；Xray-core 下载文件名修正为大写 Xray 前缀
- `src/nginx.sh`: 安装函数重构为细粒度依赖检测（nginx/certbot 分别判断）；检测配置不完整时 --force-confmiss 重装；自动生成缺失的 mime.types；crontab 操作增加 command -v 守卫
- `src/core.sh`: 修复数组随机索引越界问题；create() 增加配置缺失检测；nginx_reload 错误传递
- `src/init.sh`: 核心版本号解析方式优化；systemd service 编辑增加 EUID + 文件存在守卫
- `src/systemd.sh`: DefaultLimitNOFILE sed 正则增强，避免重复追加
- `README.md` / docs/ops/release_guide.md: 修复示例路径与仓库链接